
namespace CupkekGames.Luna
{
    public enum UIStartVisibility
    {
        Visible, // The UI is instantly visible when it starts
        FadeIn, // The UI fades in when it starts
        Invisible, // The UI starts invisible
        FadeOut, // The UI fades out when it starts
    }
}